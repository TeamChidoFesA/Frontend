import React, { Component } from 'react';
import Log from '../snippets/login/form';

class Login extends Component {
  render() {
    return (
      <div>
        <Log/>
      </div>
    );
  }
}

export default Login;
